local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/memejames/elerium-v2-ui-library//main/Library", true))()
local window = library:AddWindow("Spectra Test", {
	main_color = Color3.fromRGB(41, 74, 122), -- Color
	min_size = Vector2.new(500, 500), -- Size of the gui
	can_resize = false, -- true or false
})

local Pets = window:AddTab("Pets") -- Name of tab
Pets:Show() -- shows the tab

Pets:AddLabel("=== Buy Pets ===")

local dropdownpets = Pets:AddDropdown("Select Pets", function(text)
	if text == "Mars" then  -- Code
		print("o")
	elseif text == "Earth" then
	print("k")
	elseif text == "Iridocyclitis" then
	print("Weeeee")
	end
end)

local switchpet = Pets:AddSwitch("Buy Pets", function(bool)
	 -- toggle_god_mode(bool)
end)
switchpet:Set(false)

Pets:AddLabel("=== Buy Auras ===")

local dropdownaura = Pets:AddDropdown("Select Aura", function(text)
	if text == "Mars" then  -- Code
		print("o")
	elseif text == "Earth" then
	print("k")
	elseif text == "Iridocyclitis" then
	print("Weeeee")
	end
end)

local switchaura = Pets:AddSwitch("Buy Auras", function(bool)
	 -- toggle_god_mode(bool)
end)
switchaura:Set(false)

Pets:AddLabel("=== Buy Ultimates ===")

local dropdownultimate = Pets:AddDropdown("Select Ultimate", function(text)
	if text == "Mars" then  -- Code
		print("o")
	elseif text == "Earth" then
	print("k")
	elseif text == "Iridocyclitis" then
	print("Weeeee")
	end
end)

local switchultimate = Pets:AddSwitch("Buy Ultimate", function(bool)
	 -- toggle_god_mode(bool)
end)
switchultimate:Set(false)

Pets:AddLabel("=== Auto Evolved ===")

local dropdownevolved = Pets:AddDropdown("Select Pets", function(text)
	if text == "Mars" then  -- Code
		print("o")
	elseif text == "Earth" then
	print("k")
	elseif text == "Iridocyclitis" then
	print("Weeeee")
	end
end)

local switchevol = Pets:AddSwitch("Evolve pets", function(bool)
	 -- toggle_god_mode(bool)
end)
switchevol:Set(false)

Pets:AddLabel("=== Auto Sell ===")

local dropdownsell = Pets:AddDropdown("Select Race", function(text)
	if text == "Mars" then  -- Code
		print("o")
	elseif text == "Earth" then
	print("k")
	elseif text == "Iridocyclitis" then
	print("Weeeee")
	end
end)

local switchsell = Pets:AddSwitch("Sell Pets", function(bool)
	 -- toggle_god_mode(bool)
end)
switchsell:Set(false)

Pets:AddLabel("=== Auto Trade ===")

local dropdownpetstrade = Pets:AddDropdown("Select Pets", function(text)
	if text == "Mars" then  -- Code
		print("o")
	elseif text == "Earth" then
	print("k")
	elseif text == "Iridocyclitis" then
	print("Weeeee")
	end
end)

local dropdownpetsplayers = Pets:AddDropdown("Select Player", function(text)
	if text == "Mars" then  -- Code
		print("o")
	elseif text == "Earth" then
	print("k")
	elseif text == "Iridocyclitis" then
	print("Weeeee")
	end
end)

local switchtrade = Pets:AddSwitch("Star Trade", function(bool)
	 -- toggle_god_mode(bool)
end)
switchtrade:Set(false)

local Extras = window:AddTab("Extras") -- Name of tab
Extras:Show() -- shows the tab

local folderkiller = Extras:AddFolder("More Option Killer")

local switchpuch = folderkiller:AddSwitch("Win Punch", function(bool)
	 -- toggle_god_mode(bool)
end)
switchpuch:Set(false)

local switchv2 = folderkiller:AddSwitch("Player V2", function(bool)
	 -- toggle_god_mode(bool)
end)
switchv2:Set(false)

local switchbugpacks = folderkiller:AddSwitch("Bug Packs", function(bool)
	 -- toggle_god_mode(bool)
end)
switchbugpacks:Set(false)

local switchinmortal = folderkiller:AddSwitch("Inmortal", function(bool)
	 -- toggle_god_mode(bool)
end)
switchpuch:Set(false)

local foldergood = Extras:AddFolder("Good Mode System")

local dropdownteleport = foldergood:AddDropdown("Select Player", function(text)
	if text == "Mars" then  -- Code
		print("o")
	elseif text == "Earth" then
	print("k")
	elseif text == "Iridocyclitis" then
	print("Weeeee")
	end
end)

local switchtele = foldergood:AddSwitch("Teleport", function(bool)
	 -- toggle_god_mode(bool)
end)
switchtele:Set(false)

local switchstomp = foldergood:AddSwitch("Auto Stomp", function(bool)
	 -- toggle_god_mode(bool)
end)
switchstomp:Set(false)

local switchmode = foldergood:AddSwitch("Brawl Good Mode", function(bool)
	 -- toggle_god_mode(bool)
end)
switchmode:Set(false)

local foldergoodv2 = Extras:AddFolder("Good Mode V2")

local dropdownv2 = foldergoodv2:AddDropdown("Killer List", function(text)
	if text == "Mars" then  -- Code
		print("o")
	elseif text == "Earth" then
	print("k")
	elseif text == "Iridocyclitis" then
	print("Weeeee")
	end
end)

local switchstompv2 = foldergoodv2:AddSwitch("Auto Stomp Kill List", function(bool)
	 -- toggle_god_mode(bool)
end)
switchstompv2:Set(false)

foldergoodv2:AddLabel("")

foldergoodv2:AddLabel("Kill List: ")
